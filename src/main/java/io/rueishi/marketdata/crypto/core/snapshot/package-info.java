/**
 * Snapshot gatekeeping support for book state initialization and reset.
 *
 * <p>Classes in this package will coordinate initial snapshot acceptance,
 * pre-snapshot update drops, and book reset publication before incremental
 * updates are allowed into the normal pipeline.</p>
 */
package io.rueishi.marketdata.crypto.core.snapshot;

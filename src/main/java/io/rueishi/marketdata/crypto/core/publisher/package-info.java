/**
 * Publishing contracts for normalized gateway output.
 *
 * <p>Classes in this package will accept encoded or normalized market-data
 * messages from connector pipelines and deliver them to downstream consumers
 * while reporting backpressure and publish failures through observability.</p>
 */
package io.rueishi.marketdata.crypto.core.publisher;

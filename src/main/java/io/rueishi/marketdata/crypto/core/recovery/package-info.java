/**
 * Recovery abstractions for connector restart and resynchronization flows.
 *
 * <p>Classes in this package will coordinate reconnect attempts, recovery
 * execution, snapshot reset behavior, and failure reporting when a connector
 * detects gaps, liveness failures, or transport errors.</p>
 */
package io.rueishi.marketdata.crypto.core.recovery;

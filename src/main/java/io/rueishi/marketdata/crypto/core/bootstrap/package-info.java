/**
 * Startup orchestration package for the gateway runtime.
 *
 * <p>Future bootstrap classes in this package will load configuration, create
 * shared runtime services, wire connector instances, and start the gateway
 * system flow.</p>
 */
package io.rueishi.marketdata.crypto.core.bootstrap;
